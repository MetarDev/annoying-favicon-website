"use client";

import { Box, Code, Heading } from "@chakra-ui/react";

export const AnchorNavigation = ({}: {}) => {
  return (
    <Box position={"sticky"} flex={0}>
      Navigation goes here
    </Box>
  );
};
